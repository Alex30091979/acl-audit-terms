# 💰 Анализ биллинга и защита от расходов

## ✅ ХОРОШИЕ НОВОСТИ: Ваше расширение уже правильно настроено!

### 🔍 Анализ вашего кода

**Ваше расширение использует OAuth токены пользователя, а НЕ API ключи проекта!**

Это означает:
- ✅ **Расходы НЕ начисляются на ваш проект**
- ✅ **Расходы начисляются на квоту пользователя**
- ✅ **Вы не платите за использование API**

### Как это работает в вашем расширении:

```javascript
// Все запросы используют токен пользователя:
headers: { 'Authorization': `Bearer ${token}` }

// Токен получается через OAuth пользователя:
chrome.identity.launchWebAuthFlow({ url: authUrl, interactive: true }, ...)
```

**Это правильный подход!** ✅

---

## 📊 Разница между подходами

### ❌ НЕПРАВИЛЬНО (расходы на ваш проект):

```javascript
// Использование API ключа проекта
const API_KEY = 'YOUR_PROJECT_API_KEY';
fetch(`https://www.googleapis.com/drive/v3/files?key=${API_KEY}`, ...)

// Использование Service Account
const serviceAccount = require('./service-account.json');
// Все запросы идут от имени проекта
```

**Результат:** Все расходы начисляются на ваш Billing Account!

---

### ✅ ПРАВИЛЬНО (расходы на пользователя):

```javascript
// Использование OAuth токена пользователя
const token = await getAuthToken({ interactive: true });
fetch('https://www.googleapis.com/drive/v3/files', {
  headers: { 'Authorization': `Bearer ${token}` }
})
```

**Результат:** Расходы начисляются на квоту пользователя, а не на ваш проект!

---

## 🎯 Ответы на ваши вопросы

### 1. Можно ли переложить расходы на конечного пользователя?

**✅ ДА! Ваше расширение УЖЕ делает это!**

Когда используется OAuth токен пользователя:
- Каждый запрос API идёт от имени пользователя
- Расходы начисляются на квоту пользователя
- Вы не платите за использование API

**Ваше расширение уже использует правильный подход!** ✅

---

### 2. Как сделать, чтобы проект всегда оставался бесплатным?

**✅ Ваш проект УЖЕ будет бесплатным!**

При условии, что:
- ✅ Используются только OAuth токены пользователя (уже так)
- ✅ НЕ используются API ключи проекта
- ✅ НЕ используются Service Accounts
- ✅ НЕ используются Cloud Functions / Cloud Run / Firestore

**Ваше расширение соответствует всем условиям!** ✅

---

## ⚠️ Что нужно проверить и защитить

### 1. Убедитесь, что НЕТ API ключей в коде

**Проверка:**
```bash
# В вашем проекте НЕТ:
- API_KEY
- apiKey
- service-account.json
- ServiceAccount
```

**Статус:** ✅ Проверено - API ключей нет!

---

### 2. Настройте квоты и ограничения (на всякий случай)

**В Google Cloud Console:**

1. **Ограничьте квоты API (если они есть):**
   - Откройте: https://console.cloud.google.com/apis/api/drive.googleapis.com/quotas?project=bk-del-access2g-sheets
   - Установите лимиты на запросы в день/минуту
   - Это защита на случай, если кто-то попытается использовать API ключи

2. **Настройте бюджетные ограничения:**
   - Откройте: https://console.cloud.google.com/billing/budgets?project=bk-del-access2g-sheets
   - Создайте бюджет с лимитом $0
   - Настройте уведомления при любых расходах

3. **Отключите ненужные API:**
   - Откройте: https://console.cloud.google.com/apis/dashboard?project=bk-del-access2g-sheets
   - Отключите API, которые не используются

---

### 3. Мониторинг расходов

**Настройте алерты:**

1. **Billing Alerts:**
   - Откройте: https://console.cloud.google.com/billing/budgets?project=bk-del-access2g-sheets
   - Создайте бюджет: $0
   - Настройте уведомления на email при любых расходах

2. **API Quotas:**
   - Откройте: https://console.cloud.google.com/apis/api/drive.googleapis.com/quotas?project=bk-del-access2g-sheets
   - Установите лимиты (если доступно)

---

## 📋 Бесплатные квоты Google API (для пользователей)

### Google Drive API:
- **1,000,000 запросов/день** - бесплатно для каждого пользователя
- **1,000 запросов/100 секунд** - бесплатно

### Google Sheets API:
- **500 запросов/100 секунд** - бесплатно для каждого пользователя
- **Неограниченное чтение** - бесплатно

**Важно:** Эти квоты применяются к каждому пользователю отдельно, а не к вашему проекту!

---

## 🔒 Дополнительная защита

### 1. Проверка в коде (добавьте для безопасности)

Добавьте проверку, что токен действительно от пользователя:

```javascript
// Убедитесь, что токен получен через OAuth пользователя
async function verifyTokenIsUserToken(token) {
  const response = await fetch('https://www.googleapis.com/oauth2/v2/userinfo', {
    headers: { 'Authorization': `Bearer ${token}` }
  });
  if (!response.ok) {
    throw new Error('Invalid token');
  }
  const userInfo = await response.json();
  console.log('Token belongs to user:', userInfo.email);
  return userInfo;
}
```

**Ваше расширение уже делает это!** ✅

---

### 2. Никогда не используйте API ключи в коде

**❌ НЕ ДЕЛАЙТЕ ТАК:**
```javascript
// НИКОГДА не добавляйте это:
const API_KEY = 'YOUR_API_KEY';
fetch(`https://www.googleapis.com/drive/v3/files?key=${API_KEY}`, ...)
```

**✅ ВСЕГДА используйте OAuth токены:**
```javascript
// Правильно:
const token = await getAuthToken({ interactive: true });
fetch('https://www.googleapis.com/drive/v3/files', {
  headers: { 'Authorization': `Bearer ${token}` }
})
```

---

### 3. Не используйте Service Accounts

**❌ НЕ ДЕЛАЙТЕ ТАК:**
```javascript
// НИКОГДА не используйте Service Accounts для пользовательских запросов
const serviceAccount = require('./service-account.json');
```

**✅ ВСЕГДА используйте OAuth токены пользователя:**
```javascript
// Правильно:
const token = await getAuthToken({ interactive: true });
```

---

## 📊 Итоговая оценка рисков

### Риск расходов для вашего проекта: **МИНИМАЛЬНЫЙ** ✅

**Почему:**
- ✅ Используются только OAuth токены пользователя
- ✅ Нет API ключей проекта
- ✅ Нет Service Accounts
- ✅ Нет Cloud Functions / Cloud Run
- ✅ Все запросы идут от имени пользователя

**Расходы начисляются на:**
- ✅ Квоту пользователя (бесплатно для пользователя)
- ❌ НЕ на ваш проект

---

## ✅ Рекомендации

### Что делать СЕЙЧАС:

1. **✅ Ничего не менять в коде** - всё уже правильно!

2. **Настроить бюджетные ограничения (на всякий случай):**
   - Создайте бюджет $0
   - Настройте уведомления
   - Ссылка: https://console.cloud.google.com/billing/budgets?project=bk-del-access2g-sheets

3. **Проверить, что нет API ключей:**
   - ✅ Уже проверено - API ключей нет!

4. **Мониторить расходы (опционально):**
   - Периодически проверяйте: https://console.cloud.google.com/billing?project=bk-del-access2g-sheets
   - Должно быть $0,00

---

## 🎯 Вывод

### Ваше расширение уже правильно настроено! ✅

- ✅ Использует OAuth токены пользователя
- ✅ Расходы начисляются на пользователя, а не на проект
- ✅ Проект останется бесплатным
- ✅ Нет риска неожиданных расходов

### Единственное, что нужно сделать:

**Настроить бюджетные ограничения** для дополнительной защиты (на всякий случай).

---

## 🔗 Полезные ссылки

- **Billing Dashboard:** https://console.cloud.google.com/billing?project=bk-del-access2g-sheets
- **Budgets:** https://console.cloud.google.com/billing/budgets?project=bk-del-access2g-sheets
- **API Quotas:** https://console.cloud.google.com/apis/api/drive.googleapis.com/quotas?project=bk-del-access2g-sheets
- **APIs Dashboard:** https://console.cloud.google.com/apis/dashboard?project=bk-del-access2g-sheets

---

**Дата анализа:** 2024-12-19  
**Статус:** ✅ Безопасно настроено  
**Риск расходов:** Минимальный

